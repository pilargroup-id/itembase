const express=require('express');const C=require('../../controllers/master/variant.controller');const{authenticate,requireApp}=require('../../middleware/auth.middleware');const router=express.Router();router.use(authenticate,requireApp('itembase'));
router.get('/attributes',C.attributes);router.get('/attributes/:id',C.attribute);router.post('/attributes',C.createAttribute);router.put('/attributes/:id',C.updateAttribute);router.patch('/attributes/:id/status',C.statusAttribute);router.delete('/attributes/:id',C.deleteAttribute);
router.get('/values',C.values);router.get('/values/:id',C.value);router.post('/values',C.createValue);router.put('/values/:id',C.updateValue);router.patch('/values/:id/status',C.statusValue);router.delete('/values/:id',C.deleteValue);module.exports=router;
