# In development, let Vite proxy /api requests to the backend.
VITE_API_BASE_URL=/api

# Central login page users are redirected to when they have no valid session.
# Defaults to https://pilargroup.id/login when unset.
# VITE_LOGIN_URL=https://pilargroup.id/login
